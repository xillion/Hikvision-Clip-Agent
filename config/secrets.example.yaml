# Example secrets. Install as /etc/hikvision-agent/secrets.yaml with mode 0600.
recorders:
  nvr-1:
    password: "change-me"

clip_server:
  oauth:
    client_id: "change-me"
    client_secret: "change-me"
